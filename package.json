{
  "name": "conversor-markdown-pdf",
  "version": "1.0.0",
  "description": "Conversor local de Markdown para PDF que evita o erro 'Tainted Canvas' executando em um servidor web simples.",
  "main": "index.html",
  "scripts": {
    "start": "live-server --open=index.html --port=8080 --host=localhost"
  },
  "keywords": [
    "markdown",
    "pdf",
    "html2canvas",
    "local-server",
    "documentation",
    "converter"
  ],
  "author": "Victor Hugo",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "dependencies": {
    "html2pdf": "^0.0.11"
  }
}
